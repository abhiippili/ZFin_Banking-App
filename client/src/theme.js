import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#20232a"
    },
    secondary: {
      main: "#61dafb"
    },
    info: {
      main: "#fff"
    },
    background: {
      default: "#ffd700"
    }
  }
});
